# One-time imports for resources created manually via CloudShell.
# These blocks were removed after their first successful `terraform apply`.
# If you need to re-import resources, add temporary import blocks here.
